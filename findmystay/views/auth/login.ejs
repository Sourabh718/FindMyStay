<%- include('../partials/head') %>
</head>
<body>
    <%- include('../partials/nav') %>
    <main class="container mx-auto mt-8 p-8 bg-white rounded-lg shadow-md">
        <h1 class="text-3xl font-bold text-red-500 mb-4">Login</h1>
        <form action="/login" method="POST">

        <input type="text" name="username" placeholder="Enter your Email/Username" 
        class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"/>
    
        <input type="password" name="password" placeholder="Enter Password"
        class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"/>
        <input type="submit" value="sign in" class="w-full bg-red-500 text-white py-2 rounded-md hover:bg-red-600 transition duration-300" />
        </form>
    </main>
</body>
</html>
<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>➕ Add New Home | TrendNest</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #141e30, #243b55);
      color: #ecf0f1;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }
    .container {
      background: rgba(255, 255, 255, 0.06);
      border: 1px solid rgba(255, 255, 255, 0.15);
      border-radius: 16px;
      padding: 40px;
      width: 100%;
      max-width: 500px;
      backdrop-filter: blur(10px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
    }
    h1 {
      text-align: center;
      margin-bottom: 24px;
      font-size: 28px;
      color: #00cec9;
    }
    input[type="text"],
    input[type="number"] {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: none;
      border-radius: 8px;
      background-color: #f0f0f0;
      color: #2c3e50;
      font-size: 16px;
    }
    input[type="text"]:focus,
    input[type="number"]:focus {
      outline: 2px solid #00cec9;
    }
    input[type="submit"] {
      width: 100%;
      background: linear-gradient(to right, #00b894, #00cec9);
      color: white;
      padding: 12px;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      margin-top: 20px;
      transition: background 0.3s ease, transform 0.2s ease;
    }
    input[type="submit"]:hover {
      background: linear-gradient(to right, #00cec9, #00b894);
      transform: scale(1.03);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>➕ Add a Futuristic Home</h1>
    <form action="/host/add-home" method="POST">
      <input type="text" name="houseName" placeholder="Home Name" required>
      <input type="text" name="location" placeholder="City / Location" required>
      <input type="number" name="price" placeholder="Nightly Price (₹)" required>
      <input type="text" name="photoUrl" placeholder="Image URL" required>
      <input type="number" name="rating" step="0.1" max="5" placeholder="Rating (e.g. 4.5)" required>
      <input type="submit" value="Register Home">
    </form>
  </div>
</body>
</html> -->
